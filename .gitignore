# Dependencies
node_modules/

# Superpowers brainstorming
.superpowers/

# Git worktrees
.worktrees/
.claude/worktrees/

# Build outputs
dist-electron/
dist-react/
dist-web/
dist/
dist_out/
release/
tile-work/

# Dev web report build artifacts (copied from dist-web/ by dev upload)
web/assets/
web/img/
web/svg/
web/web/
web/web-report-themes/
web/logo.png
web/logo.json
web/report.json
web/theme.json
web/ui-theme.json

# Logs
*.log
*.evtc
*.zevtc
*.csv
# Narrow, owner-authorized exception to the blanket *.zevtc PII guard above:
# the anonymized axilog integration fixture, so the real-parse tests run in CI.
# Every character name in it is `Anon<N>`, every account `:Anon<N>.<digits>`,
# every guild id zeroed — verified before commit. `.anon.` is required in the
# name, so a raw capture dropped in this directory stays ignored.
!test-fixtures/axilog/*.anon.zevtc

# Environment variables
.env
.env.*

# OS generated files
.DS_Store
Thumbs.db
.cache/
.npm/

# IDEs/Editors
.vscode/
.idea/
*.swp
*.swo

# Local stores
.electron-store/

# Playwright
test-results/
playwright-report/

# Test Data
testdata/

# Dev web report testing
web/reports/
dev/

axibridge-settings.json
arcbridge-settings.json
graphify-out/
repomix-output.txt
.playwright-mcp/

# Local scratch probes (never committed)
/scratch/

# axi-design screenshot harness scratch
.axi-mocks/
share-viewer.png
